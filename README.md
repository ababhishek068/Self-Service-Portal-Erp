# SelfServiceErp

Enterprise admin console for Hijra Bank Self Service. See **[self-service-erp/README.md](./self-service-erp/README.md)** for setup and usage.

```bash
cd self-service-erp && npm install && npm run dev
```
